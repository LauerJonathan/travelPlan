import { Hotel, Home, Umbrella, Anchor, Bus, Car, Train, Plane, Ship, Bike } from 'lucide-react';

export const accommodationIcons = {
  hotel: Hotel,
  house: Home,
  bungalow: Umbrella,
  boat: Anchor,
};

export const transportIcons = {
  bus: Bus,
  car: Car,
  train: Train,
  plane: Plane,
  ship: Ship,
  scooter: Bike,
};